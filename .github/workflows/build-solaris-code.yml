name: ESP-IDF Build
on:
  push:
    paths-ignore:
      - 'website/**'

env:
  CI_IMAGE: solaris-ci:latest
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-ci-image:
    runs-on: self-hosted
    steps:
      - name: Clean workspace
        run: |
          sudo chown -R "$(id -u):$(id -g)" "${{ github.workspace }}" 2>/dev/null || true
          sudo rm -rf "${{ github.workspace }}"/* "${{ github.workspace }}"/.??* 2>/dev/null || true

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.CI_GH_TOKEN }}

      - name: Build CI image if needed
        run: |
          LOCAL_HASH=$(sha256sum .github/Dockerfile.ci | cut -d' ' -f1)
          REMOTE_HASH=$(docker inspect "$CI_IMAGE" --format '{{index .Config.Labels "dockerfile.hash"}}' 2>/dev/null || echo "")
          if [ "$LOCAL_HASH" != "$REMOTE_HASH" ]; then
            echo "Rebuilding CI image..."
            docker build -f .github/Dockerfile.ci --label "dockerfile.hash=$LOCAL_HASH" -t "$CI_IMAGE" .
          else
            echo "CI image is up to date"
          fi

  check-spp-changes:
    runs-on: self-hosted
    outputs:
      changed: ${{ steps.filter.outputs.changed }}
    steps:
      - name: Clean workspace
        run: |
          sudo chown -R "$(id -u):$(id -g)" "${{ github.workspace }}" 2>/dev/null || true
          sudo rm -rf "${{ github.workspace }}"/* "${{ github.workspace }}"/.??* 2>/dev/null || true

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          token: ${{ secrets.CI_GH_TOKEN }}

      - name: Check for SPP submodule changes
        id: filter
        run: |
          if git diff --name-only HEAD~1 HEAD | grep -q '^solaris-v2/spp'; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

  build-solaris-code:
    needs: build-ci-image
    runs-on: self-hosted
    steps:
      - name: Clean workspace
        run: |
          sudo chown -R "$(id -u):$(id -g)" "${{ github.workspace }}" 2>/dev/null || true
          sudo rm -rf "${{ github.workspace }}"/* "${{ github.workspace }}"/.??* 2>/dev/null || true

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false
          persist-credentials: false
          token: ${{ secrets.CI_GH_TOKEN }}

      - name: Prepare submodules
        run: |
          git config --global --add safe.directory "${{ github.workspace }}"
          rm -rf .git/modules/* || true
          rm -rf solaris-v2/esp-idf solaris-v2/spp || true
          if [ -f .gitmodules ]; then
            sed -i \
              -e 's#git@github.com:#https://github.com/#g' \
              -e 's#ssh://git@github.com:#https://github.com/#g' \
              .gitmodules
          fi
          git config --global url."https://x-access-token:${{ secrets.CI_GH_TOKEN }}@github.com/".insteadOf "https://github.com/"
          git config --global url."https://x-access-token:${{ secrets.CI_GH_TOKEN }}@github.com/".insteadOf "git@github.com:"
          git config --global url."https://x-access-token:${{ secrets.CI_GH_TOKEN }}@github.com/".insteadOf "ssh://git@github.com/"
          git submodule sync --recursive
          git submodule update --init solaris-v2/spp
          git submodule add -f https://github.com/espressif/esp-idf.git solaris-v2/esp-idf
          cd solaris-v2/esp-idf
          git checkout v5.1

      - name: Build firmware
        run: |
          docker run --rm --privileged \
            --user "$(id -u):$(id -g)" \
            -e IDF_CCACHE_ENABLE=0 \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace/solaris-v2 \
            $CI_IMAGE \
            bash -c '. /opt/esp/idf/export.sh && idf.py build'

  static-analysis:
    needs: build-ci-image
    runs-on: self-hosted
    steps:
      - name: Clean workspace
        run: |
          sudo chown -R "$(id -u):$(id -g)" "${{ github.workspace }}" 2>/dev/null || true
          sudo rm -rf "${{ github.workspace }}"/* "${{ github.workspace }}"/.??* 2>/dev/null || true

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: true
          token: ${{ secrets.CI_GH_TOKEN }}

      - name: Run cppcheck with MISRA
        run: |
          docker run --rm --privileged \
            --user "$(id -u):$(id -g)" \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            $CI_IMAGE \
            bash -c '
              set -e
              mkdir -p doc/cppcheck
              python3 scripts/misra_ruletexts.py --all-rules \
                --pointer-edition "MISRA C:2025" -o misra_rules.generated.txt
              cppcheck \
                --addon=.github/misra-addon.json \
                --enable=all \
                --std=c11 \
                --error-exitcode=0 \
                --suppress=missingIncludeSystem \
                --suppress=unusedFunction \
                --xml \
                $(python3 scripts/cppcheck_includes.py solaris-v2/spp solaris-v2/main) \
                -i solaris-v2/spp/tests \
                solaris-v2/main/main.c \
                solaris-v2/spp \
                2> cppcheck-result.xml
              cppcheck-htmlreport \
                --file=cppcheck-result.xml \
                --title="Solaris MISRA-C:2025 Report" \
                --report-dir=doc/cppcheck \
                --source-dir=.
            '

      - name: Upload MISRA report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: misra-report
          path: doc/cppcheck/
          
  generate-docs:
    needs: build-ci-image
    runs-on: self-hosted
    steps:
      - name: Clean workspace
        run: |
          sudo chown -R "$(id -u):$(id -g)" "${{ github.workspace }}" 2>/dev/null || true
          sudo rm -rf "${{ github.workspace }}"/* "${{ github.workspace }}"/.??* 2>/dev/null || true

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: false
          token: ${{ secrets.CI_GH_TOKEN }}

      - name: Generate documentation
        run: |
          docker run --rm --privileged \
            --user "$(id -u):$(id -g)" \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            $CI_IMAGE \
            bash -c 'doxygen Doxyfile'

      - name: Upload documentation
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: doxygen-documentation
          path: doc/html/
