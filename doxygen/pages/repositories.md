# Repositories {#repositories}

Solaris is two repositories: `solaris-software` (everything platform-dependent,
plus the tooling) and `solaris-packet-protocol` (everything portable), pulled in
as a submodule. These pages cover that split and the CMake build behind it.

- @subpage architecture
- @subpage build-system
